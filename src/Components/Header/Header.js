import React from "react";
import classes from './Header.module.css'
import Button from "../UI/Button/Button";

const Header = (props)=>(
	<div className="App-header">
		<div className="App-title">

		</div>
		<div className="App-header_btn">
		</div>
	</div>
)
export default Header;
